/* eslint-disable import/prefer-default-export */
import styled from "styled-components"

export const Holder = styled.div`
  border-top: 1px solid ${({ theme }) => theme.colors.greyscale.lighter};

  padding: 1.5rem 2rem;
  width: 100vw;
`
